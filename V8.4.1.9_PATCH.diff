--- /mnt/data/adam8419_orig/app.py	2026-09-10 19:54:17.000000000 +0000
+++ /mnt/data/adam8419/app.py	2026-09-10 20:29:36.248632731 +0000
@@ -188,7 +188,7 @@
 from adam_core.meeting_language_policy import DEFAULT_REPLY_LANGUAGE, detect_requested_reply_language, tts_locale_for_reply, professional_scope_prompt
 from adam_core.meeting_focus_engine import evaluate_meeting_focus, detect_explicit_project_mismatch, self_test as meeting_focus_self_test
 
-VERSION = "v8.4.1.8"
+VERSION = "v8.4.1.9"
 BASELINE_VERSION = "v0.8.0.1"
 BUILD_CHANNEL = "acquisition"
 
@@ -7659,6 +7659,38 @@
     }
 
 
+def _meeting_latency_fast_factual_turn(question, direct_question=True, clarification_followup=False, followup_priority=False, turn_kind="question", proactive_reason=""):
+    """Return True only for short, self-contained factual questions safe for a compact prompt.
+
+    v8.4.1.9 keeps governance/change/approval turns on the full meeting prompt. The
+    fast path is intentionally narrow so latency work cannot weaken owner authority,
+    clarification behavior, contextual follow-ups, or consequential-change handling.
+    """
+    q = re.sub(r"\s+", " ", str(question or "").strip().lower())
+    if not q or not direct_question or clarification_followup or followup_priority:
+        return False
+    if str(turn_kind or "question").strip().lower() != "question" or str(proactive_reason or "").strip():
+        return False
+    if len(q) > 360:
+        return False
+    consequential = (
+        "approve", "approval", "approved", "proceed", "authorize", "authorise",
+        "variation", "cost", "payment", "commit", "change", "replace", "replacement",
+        "increase", "decrease", "reduce", "revise", "revision", "accept", "start tomorrow",
+    )
+    if any(term in q for term in consequential):
+        return False
+    factual_patterns = (
+        r"\bwhat\s+is\s+the\s+function\s+of\b",
+        r"\bwhat\s+is\s+the\s+purpose\s+of\b",
+        r"\bwhat\s+does\b.{0,80}\bdo\b",
+        r"\bhow\s+does\b.{0,80}\bwork\b",
+        r"\bwhy\s+(?:is|are|do|does)\b",
+        r"\bexplain\b",
+    )
+    return any(re.search(pattern, q, re.I) for pattern in factual_patterns)
+
+
 def _meeting_explicit_owner_approval_gate(question, answer, status):
     """Return True only when the paired exchange itself carries an owner approval gate.
 
@@ -8194,12 +8226,35 @@
         "\n\nRecent meeting conversation:\n" + ("\n".join(history_lines) or "(none yet)") +
         "\n\nParticipant interaction:\n" + question
     )
+    # v8.4.1.9 — narrow low-latency factual path. Governance, approval/change,
+    # clarification, proactive, and contextual-follow-up turns keep the full prompt.
+    latency_fast_path = _meeting_latency_fast_factual_turn(
+        question, direct_question=direct_question, clarification_followup=clarification_followup,
+        followup_priority=followup_priority, turn_kind=turn_kind, proactive_reason=proactive_reason,
+    )
+    meeting_max_tokens = 320 if latency_fast_path else 700
+    if latency_fast_path:
+        compact_context = effective_meeting_context[-3500:]
+        compact_briefing = owner_briefing[-2200:]
+        prompt = (
+            "You are Adam, the owner's disclosed AI meeting representative in an owner-approved live meeting. "
+            "Answer this short factual or technical question quickly and professionally. Give the conclusion first and keep the normal reply to about 1-3 concise sentences unless a little more is necessary for accuracy. "
+            "Use only facts supported by the current meeting context or general technical knowledge. If the question asks for a project-specific approved value that is not in the context, say it cannot be confirmed and name the required record. "
+            "Never invent approvals, dates, costs, commitments, owner positions, or project facts. Never approve, authorize, accept a variation/cost, or make a consequential commitment on Mohamad's behalf. "
+            "For machine routing, start with exactly ANSWER: when clear or CLARIFY: only when the question itself is genuinely ambiguous. Do not add any other label. "
+            "Reply in " + reply_language + "."
+            "\n\nOwner briefing (use only if directly relevant):\n" + (compact_briefing or "(none)") +
+            "\n\nRecent live meeting context (use only if directly relevant):\n" + (compact_context or "(none)") +
+            "\n\nParticipant question:\n" + question
+        )
+
     # v8.4.1.8 — deterministic decision-critical clarification gate. This runs
     # before the provider so a clear but under-specified pump-change approval
     # question cannot be marked Answered merely because Adam gave a safe
     # conditional recommendation.
     material_change_clarity = _meeting_material_change_clarification(question, direct_question)
     forced_material_clarification = bool(material_change_clarity.get("required"))
+    provider_elapsed_ms = 0
     if forced_material_clarification:
         missing_text = ", ".join(material_change_clarity.get("missing") or [])
         if reply_language.lower().startswith("ar"):
@@ -8223,7 +8278,9 @@
                 }), 409
             _MEETING_AI_INFLIGHT.add(flight_key)
         try:
-            raw_answer = call_ai(prompt, reply_language, max_tokens=700, timeout=25).strip()
+            provider_started_at = time.perf_counter()
+            raw_answer = call_ai(prompt, reply_language, max_tokens=meeting_max_tokens, timeout=25).strip()
+            provider_elapsed_ms = int((time.perf_counter() - provider_started_at) * 1000)
             if raw_answer:
                 _meeting_accepted_turn_upsert(
                     accepted_turns, question_id=question_id, text=question, focus_status="accepted",
@@ -8384,6 +8441,9 @@
         "session_id_present": bool(session_id),
         "clarification_requested": clarification_requested,
         "material_change_clarification_gate": forced_material_clarification,
+        "latency_fast_path": latency_fast_path,
+        "provider_elapsed_ms": provider_elapsed_ms,
+        "meeting_max_tokens": meeting_max_tokens,
         "ignore_requested": ignore_requested,
         "answer_kind": answer_kind,
     })
--- /mnt/data/adam8419_orig/templates/real_meeting_attendance.html	2026-09-10 19:53:11.000000000 +0000
+++ /mnt/data/adam8419/templates/real_meeting_attendance.html	2026-09-10 20:31:05.731901125 +0000
@@ -13,13 +13,13 @@
 <label>Live transcript / manual notes</label><textarea id="transcript" placeholder="Live transcript will appear here. You can also paste meeting transcript or notes manually."></textarea>
 <div class="note" id="typedMeetingQueryPanel" style="margin-top:10px"><b>Typed meeting query / comment / order</b><p style="margin:6px 0"><small>Use this box when you want Adam to answer typed meeting input. It uses the same Meeting Focus, project isolation, meeting memory, question coverage, and owner-approval rules as microphone speech. Press Enter or click Send Typed Query.</small></p><div style="display:flex;gap:8px;align-items:center"><input id="meetingTextQuery" type="text" style="flex:1" placeholder="Example: The contractor wants to increase the chilled-water pipe from 100 mm to 125 mm. What do you think?"><button id="sendMeetingTextQuery" type="button">Send Typed Query</button></div><p id="meetingTextQueryStatus" style="margin:6px 0 0">Typed meeting input: ready</p></div>
 <button id="start">Start Listening</button><button id="stop" class="stop">Stop Listening</button><button id="analyze" class="secondary">Analyze Meeting Notes</button><button id="clear" class="secondary">Clear Screen</button><button id="endMeeting" class="stop">End Meeting / New Session</button>
-<p id="micStatus">Microphone: idle</p><p id="autoStatus"><b>Representative auto-response:</b> not armed</p><p id="meetingReplyTrace"><b>Meeting reply trace:</b> ready</p><pre id="out">Ready.</pre>
+<p id="micStatus">Microphone: idle</p><p id="autoStatus"><b>Representative auto-response:</b> not armed</p><p id="meetingReplyTrace"><b>Meeting reply trace:</b> ready</p><p id="meetingLatencyStatus"><b>Meeting response latency:</b> ready</p><pre id="out">Ready.</pre>
 <div id="universalAssistantPanel" class="ok" style="margin-top:16px"><b>Adam — Universal AI Assistant</b><p><b>Normal assistant mode:</b> ask Adam anything by text or voice. Universal Assistant is independent of Meeting Focus Lock, project isolation, and meeting-question filters. It keeps one active task/thread at a time so an old subject is not mixed into a new one. External actions still use Adam's normal owner-approval boundaries.</p><div class="row"><div><label>Current task / subject focus</label><input id="universalTaskFocus" type="text" placeholder="Example: Prepare RFI reply / Review HVAC issue / General questions"></div><div><label>Assistant response</label><select id="universalResponseLanguage"><option value="auto" selected>Auto — match my request</option><option value="English">English</option><option value="Arabic">Arabic</option></select></div></div><label>Ask Adam anything / give an order</label><textarea id="universalInput" style="min-height:85px" placeholder="Example: Explain this technical issue, draft a letter, check my saved contact, prepare an email, or ask any general question."></textarea><button id="universalAsk">Ask Adam</button><button id="universalVoice" class="secondary">🎙 Voice Query</button><button id="universalNewTask" class="secondary">Start New Task</button><div class="check"><input id="universalContinuityMemory" type="checkbox" checked><label for="universalContinuityMemory" style="margin:0">Use persistent owner memory across Adam upgrades (explicit corrections / “remember this” only)</label></div><div class="check"><input id="universalSpeak" type="checkbox" checked><label for="universalSpeak" style="margin:0">Speak Adam's answer automatically</label></div><p id="universalStatus">Universal Assistant: ready — no meeting approval required for normal Q&amp;A</p><label>Adam answer</label><textarea id="universalReply" readonly style="min-height:120px" placeholder="Adam's latest answer appears here."></textarea><label>Current task conversation — retained until Start New Task</label><textarea id="universalHistory" readonly style="min-height:150px" placeholder="Only the current task thread is kept here. Start New Task clears it so subjects do not mix."></textarea><label>Prepared action / review status</label><textarea id="universalAction" readonly style="min-height:90px" placeholder="If your request prepares an email, calendar item, WhatsApp, workflow, contact update, follow-up, or another action, Adam shows the prepared/review state here. Consequential external actions remain subject to normal owner approval."></textarea><p><a href="/">Open Main Adam Assistant for full action-review controls</a></p></div>
 <div id="participantPanel" class="note" style="display:none"><b>Active Participant — governed response</b><p>In Active Participant mode Adam prepares a response first and speaking uses the session approval below. In Owner Representative mode, one explicit representative approval arms automatic answers to participants who address “Adam” / “آدم”; consequential actions still require their own separate owner approval. Adam identifies himself as AI.</p><label>Latest Adam response / owner edit</label><textarea id="responseDraft" style="min-height:90px" placeholder="Adam's latest reply appears here and remains visible until another reply or End Meeting / New Session."></textarea><label>Adam reply history — retained for this meeting</label><textarea id="responseHistory" readonly style="min-height:150px" placeholder="Adam's spoken replies and clarification questions will remain here for the full meeting session. Clear Screen does not remove them."></textarea><label>Participant questions / requests / corrections — retained for this meeting</label><textarea id="questionHistory" readonly style="min-height:105px" placeholder="Meaningful questions and requests Adam handled will remain here. Background noise, likely side-room conversations, and greetings are not added when Meeting Focus Lock is enabled."></textarea><label>Meeting Discussion Record — paired participant + Adam / topic / status</label><textarea id="discussionRecord" readonly style="min-height:210px" placeholder="Each meaningful participant question is paired with Adam's answer, topic, and status. Short follow-ups stay attached to the immediately preceding discussion thread."></textarea><label>Live Meeting Register — actions / risks / approvals</label><textarea id="meetingRegister" readonly style="min-height:150px" placeholder="Adam will classify open actions, information requests, risks, and items that still require owner approval. This register never creates an approval by itself."></textarea><label>Question Coverage Monitor — every direct question accounted for</label><textarea id="questionCoverage" readonly style="min-height:150px" placeholder="Every substantive direct participant question will appear here as Answered, Answered — information unavailable, Clarification requested, or Unanswered. Saying only “Adam” or a question preamble does not create a coverage item. Adam retries one temporary AI failure automatically instead of silently dropping the question."></textarea><div class="ok" style="margin-top:12px"><b>Professional Meeting Report</b><p>Generate the report from the retained paired discussion record and live register, including context that remains after Clear Screen. The report distinguishes decisions, recommendations, open items, information required, and items needing owner approval.</p><button id="generateReport">Generate Meeting Report</button><button id="downloadReport" class="secondary">Download Word (.docx)</button><button id="printReport" class="secondary">Print / Save PDF</button><textarea id="meetingReport" readonly style="min-height:260px" placeholder="Generated meeting report will appear here."></textarea></div><button id="prepareResponse" class="secondary">Prepare Response</button><div class="check" id="manualSpeakApprovalRow"><input id="speakApproved" type="checkbox"><label for="speakApproved" style="margin:0">Owner Approval — allow Adam to speak this response</label></div><button id="authorizeSpeak">Authorize & Speak</button><p id="speakStatus">Speaking: not authorized</p>
 <div id="representativePanel" class="ok" style="display:none;margin-top:14px"><b>Owner Representative — professional rehearsal mode</b><p>Adam may represent Mohamad in professional discussion across disciplines, answer questions, explain, ask for clarification, and state only supported facts or positions written in the briefing below. Adam understands multilingual/code-switched speech. His default reply is English; a participant may explicitly ask him to switch to Arabic or another language. Adam must disclose that he is an AI representative and cannot approve costs, variations, contractual changes, purchases, payments, committed dates, or other consequential commitments without separate owner approval.</p><p><b>Natural meeting dialogue:</b> ON — Adam answers like an experienced professional colleague: conclusion first, concise by default, conversational follow-ups, calm disagreement, minimal robotic preambles, and practical clarification only when needed.</p><div class="note" id="meetingFocusPanel"><b>Human Meeting Presence — Intelligent Meeting Focus</b><div class="check"><input id="meetingFocusLock" type="checkbox" checked><label for="meetingFocusLock" style="margin:0">Meeting Focus Lock — concentrate on this meeting and reject likely side-room / other-project speech before formal transcript, memory, Q&amp;A, and report</label></div><div class="row"><div><label>Room focus sensitivity</label><select id="focusSensitivity"><option value="strict" selected>Strict — shared room / nearby conversations</option><option value="balanced">Balanced — normal meeting room</option><option value="open">Open — capture all detected speech</option></select></div><div><label>Meeting focus / agenda keywords</label><input id="meetingFocusAgenda" type="text" placeholder="Example: MEP coordination, Tower, HVAC, chilled water, ductwork, variations"></div></div><p><small>Focus Lock combines meeting title/project, declared participants/companies, current discussion context, topic relevance, and a weak near-field audio hint. In Strict mode, an explicitly named different project is a hard reject unless the speaker clearly frames it as a comparison/reference to the active project. Where the browser supports it, microphone voice isolation is requested and automatic gain is disabled so distant voices are less likely to dominate. This version does not claim biometric speaker identification or perfect physical source separation. Audio may still be processed transiently for speech transcription; filtered side conversations are not added to Adam's formal meeting transcript or retained meeting memory.</small></p><label>Selective Listening Monitor</label><textarea id="focusMonitor" readonly style="min-height:120px" placeholder="Adam will show accepted meeting speech vs filtered side-room speech here without retaining the rejected conversation text."></textarea></div><label>Representative participation style</label><select id="participationStyle"><option value="conservative">Conservative — direct questions only</option><option value="balanced" selected>Balanced — questions + material approval/commitment risks</option><option value="proactive">Proactive — also flag material technical/commercial risks and owner actions</option></select><p style="margin-top:6px"><small>Balanced is recommended. Adam should not interrupt routine discussion; proactive intervention is limited to material risks, commitments, contradictions, or owner actions.</small></p><label>Professional discipline</label><select id="professionalScope"><option value="auto" selected>Auto-detect — cross-disciplinary professional</option><option value="engineering_mep">Engineering / MEP / Construction</option><option value="project_management">Project Management</option><option value="commercial_contracts">Commercial / Contracts</option><option value="procurement">Procurement</option><option value="finance">Finance / Cost</option><option value="it_software">IT / Software</option><option value="sales_business">Sales / Business</option><option value="other">Other professional discipline</option></select><label>Owner briefing / positions Adam may rely on</label><textarea id="ownerBriefing" style="min-height:120px" placeholder="Example: Mechanical shop drawings are targeted for Tuesday. Do not accept any variation or cost. Ask the contractor to submit supporting documents for owner review."></textarea><label>Authority profile</label><textarea id="authorityProfile" style="min-height:90px">Answer technical and factual questions; ask for clarification; take notes; record requests for Mohamad. Do not approve variations, costs, contractual changes, purchases, payments, commitments, or date changes without separate owner approval.</textarea><div class="check" id="representativeApprovalRow" style="display:none"><input id="representativeApproved" type="checkbox"><label for="representativeApproved" style="margin:0">Owner representative session approval (synchronized with the main approval above)</label></div><p><b>Test operation:</b> use a second device/person as the meeting participant. Ask questions aloud or type them below. Adam should answer, speak, take notes, and refuse consequential commitments.</p></div><hr><b>Interactive Meeting Conversation</b><p>Approve once for this meeting session. Adam can then answer questions addressed to “Adam” and continue follow-up discussion. External actions and commitments still require their normal separate approvals. Do not promise, send, approve, purchase, trade, schedule, or execute external actions without the normal separate owner approval.</p>
 <div class="check"><input id="conversationApproved" type="checkbox"><label for="conversationApproved" style="margin:0">Owner Approval — allow Adam to converse during this meeting session (required for Active Participant; Owner Representative uses the representative approval above)</label></div>
 <label>Question / discussion point for Adam</label><textarea id="conversationInput" style="min-height:70px" placeholder="Example: Adam, explain why the chilled-water pipe size may need to increase."></textarea>
-<button id="askAdam">Ask Adam & Speak</button><div class="check"><input id="autoRespond" type="checkbox"><label for="autoRespond" style="margin:0">Auto-respond when a final microphone sentence addresses “Adam” (automatic in Owner Representative mode)</label></div><div class="check"><input id="meetingAutoSpeak" type="checkbox" checked><label for="meetingAutoSpeak" style="margin:0">Speak Adam's meeting answers automatically — applies to microphone and typed meeting queries</label></div><div class="check"><input id="meetingFastVoice" type="checkbox" checked><label for="meetingFastVoice" style="margin:0">Fast meeting voice — stream long answers in short speech chunks with automatic browser fallback</label></div><div class="check"><input id="meetingSpeakerProtection" type="checkbox" checked><label for="meetingSpeakerProtection" style="margin:0">Laptop speaker protection — ignore Adam's own speaker audio while he talks so the microphone does not cancel his reply (recommended)</label></div><button id="testMeetingVoice" type="button" class="secondary">Test / Enable Meeting Voice</button><p id="meetingVoiceStatus">Meeting voice: ready — click Start Listening or Test / Enable Meeting Voice once to unlock browser audio</p>
+<button id="askAdam">Ask Adam & Speak</button><div class="check"><input id="autoRespond" type="checkbox"><label for="autoRespond" style="margin:0">Auto-respond when a final microphone sentence addresses “Adam” (automatic in Owner Representative mode)</label></div><div class="check"><input id="meetingAutoSpeak" type="checkbox" checked><label for="meetingAutoSpeak" style="margin:0">Speak Adam's meeting answers automatically — applies to microphone and typed meeting queries</label></div><div class="check"><input id="meetingFastResponse" type="checkbox" checked><label for="meetingFastResponse" style="margin:0">Fast meeting response — shorter silence boundary for short questions; longer turns keep the full natural-pause protection</label></div><div class="check"><input id="meetingFastVoice" type="checkbox" checked><label for="meetingFastVoice" style="margin:0">Fast meeting voice — stream long answers in short speech chunks with automatic browser fallback</label></div><div class="check"><input id="meetingSpeakerProtection" type="checkbox" checked><label for="meetingSpeakerProtection" style="margin:0">Laptop speaker protection — ignore Adam's own speaker audio while he talks so the microphone does not cancel his reply (recommended)</label></div><button id="testMeetingVoice" type="button" class="secondary">Test / Enable Meeting Voice</button><p id="meetingVoiceStatus">Meeting voice: ready — click Start Listening or Test / Enable Meeting Voice once to unlock browser audio</p>
 <p id="conversationStatus">Conversation: waiting for session approval</p></div></div>
 <div class="card"><a href="/">Back to Adam</a> &nbsp; <a href="/meeting-platform">Meeting Platform Adapter</a></div></div>
 <script>
@@ -40,12 +40,12 @@
 // and continues listening while he is speaking so participant interruptions are captured safely.
 let meetingAudioContext=null; let meetingAnalyser=null; let meetingLevelTimer=null;
 let meetingChunkSpeechFrames=0; let meetingNoiseFloor=0.003; let meetingPeakRms=0; let meetingVadAvailable=false;
-let meetingUtteranceSpeechStarted=false;let meetingUtteranceLastSpeechAt=0;let meetingRecorderStartedAt=0;let meetingLastParticipantSpeechAt=0;
+let meetingUtteranceSpeechStarted=false;let meetingUtteranceFirstSpeechAt=0;let meetingUtteranceLastSpeechAt=0;let meetingRecorderStartedAt=0;let meetingLastParticipantSpeechAt=0;
 let meetingTranscriptionsPending=0;let meetingMemorySyncQueue=Promise.resolve();
 let meetingQuestionQueue=[];let meetingAdamSpeaking=false;let meetingAdamAudio=null;let meetingAdamAudioUrl="";let meetingAdamSpeechResolve=null;let meetingLastSpokenText="";let meetingLastSpokenAt=0;let meetingAdamAudioContextSource=null;
 let meetingVoiceUnlocked=false;let meetingVoiceUnlockContext=null;let meetingVoiceJobSeq=0;let meetingSpeakerOutputActive=false;let meetingRecorderContainsAdamAudio=false;let meetingLastVoiceDeliveryOk=false;
 let meetingRecorderStopReason='';let meetingLastTranscriptText='';let meetingLastTranscriptAt=0;
-const MEETING_END_OF_TURN_SILENCE_MS=4200;const MEETING_RESPONSE_STABILITY_MS=650;const MEETING_FINAL_TURN_MERGE_GRACE_MS=1500;const MEETING_FINAL_TURN_MAX_WAIT_MS=10000;const MEETING_CONTINUATION_SPEECH_HOLD_MS=900;const MEETING_MAX_AUTOREPLY_WAIT_MS=6000;const MEETING_SILENT_CHUNK_MS=8000;const MEETING_MAX_UTTERANCE_MS=60000;const MEETING_CLARIFICATION_WINDOW_MS=45000;
+const MEETING_END_OF_TURN_SILENCE_MS=4200;const MEETING_FAST_END_OF_TURN_SILENCE_MS=2400;const MEETING_FAST_TURN_MAX_SPEECH_MS=6500;const MEETING_RESPONSE_STABILITY_MS=650;const MEETING_FINAL_TURN_MERGE_GRACE_MS=1500;const MEETING_FINAL_TURN_FAST_COMPLETE_GRACE_MS=350;const MEETING_FINAL_TURN_MAX_WAIT_MS=10000;const MEETING_CONTINUATION_SPEECH_HOLD_MS=900;const MEETING_MAX_AUTOREPLY_WAIT_MS=6000;const MEETING_SILENT_CHUNK_MS=8000;const MEETING_MAX_UTTERANCE_MS=60000;const MEETING_CLARIFICATION_WINDOW_MS=45000;const MEETING_FAST_VOICE_CHUNK_CHARS=140;
 const transcript=document.getElementById('transcript'); const micStatus=document.getElementById('micStatus'); const modeEl=document.getElementById('mode'); const participantPanel=document.getElementById('participantPanel');
 const meetingDateTimeEl=document.getElementById('meetingDateTime');if(meetingDateTimeEl&&!meetingDateTimeEl.value){const d=new Date();const local=new Date(d.getTime()-d.getTimezoneOffset()*60000);meetingDateTimeEl.value=local.toISOString().slice(0,16);}
 let meetingAutoResponseBusy=false;let meetingAutoInFlightKey='';let meetingLastAutoQuestion='';let meetingLastAutoAt=0;let meetingPendingAutoParts=[];let meetingPendingAutoQuestionId='';let meetingPendingAutoTimer=null;let meetingPendingAutoStartedAt=0;let meetingLastAcceptedTranscriptAt=0;let meetingLastFocusCandidateText='';let meetingLastFocusCandidateAt=0;
@@ -91,6 +91,25 @@
  const idx=meetingReplyTraceSteps.findIndex(x=>x.step===Number(step));const item={step:Number(step),label:String(label||'')};if(idx>=0)meetingReplyTraceSteps[idx]=item;else meetingReplyTraceSteps.push(item);meetingReplyTraceSteps.sort((a,b)=>a.step-b.step);
  const el=document.getElementById('meetingReplyTrace');if(el)el.innerHTML='<b>Meeting reply trace:</b> '+meetingReplyTraceSteps.map(x=>x.step+' '+x.label).join(' → ');
 }
+let meetingLatencyTrace={speechEndAt:0,sttStartAt:0,sttEndAt:0,dispatchAt:0,aiStartAt:0,aiEndAt:0,voiceStartAt:0,questionId:'',providerMs:0,fastPath:false};
+function resetMeetingLatencyTrace(){meetingLatencyTrace={speechEndAt:0,sttStartAt:0,sttEndAt:0,dispatchAt:0,aiStartAt:0,aiEndAt:0,voiceStartAt:0,questionId:'',providerMs:0,fastPath:false};const el=document.getElementById('meetingLatencyStatus');if(el)el.innerHTML='<b>Meeting response latency:</b> ready';}
+function renderMeetingLatencyTrace(){
+ const t=meetingLatencyTrace,el=document.getElementById('meetingLatencyStatus');if(!el)return;
+ const sec=ms=>(Math.max(0,Number(ms)||0)/1000).toFixed(1)+'s';
+ const total=(t.voiceStartAt&&t.speechEndAt)?t.voiceStartAt-t.speechEndAt:0;
+ const endTurn=(t.sttStartAt&&t.speechEndAt)?t.sttStartAt-t.speechEndAt:0;
+ const stt=(t.sttEndAt&&t.sttStartAt)?t.sttEndAt-t.sttStartAt:0;
+ const focusMerge=(t.aiStartAt&&t.sttEndAt)?t.aiStartAt-t.sttEndAt:0;
+ const ai=(t.aiEndAt&&t.aiStartAt)?t.aiEndAt-t.aiStartAt:0;
+ const voicePrep=(t.voiceStartAt&&t.aiEndAt)?t.voiceStartAt-t.aiEndAt:0;
+ const parts=[];if(total)parts.push('total to voice start '+sec(total));if(endTurn)parts.push('end-turn '+sec(endTurn));if(stt)parts.push('STT '+sec(stt));if(focusMerge)parts.push('focus/merge '+sec(focusMerge));if(ai)parts.push('AI '+sec(ai));if(voicePrep)parts.push('voice prep '+sec(voicePrep));if(t.providerMs)parts.push('provider '+sec(t.providerMs));if(t.fastPath)parts.push('fast factual path');
+ el.innerHTML='<b>Meeting response latency:</b> '+(parts.length?parts.join(' | '):'measuring…');
+}
+function meetingTextLooksCompleteForFastReply(text){
+ const t=String(text||'').trim();if(!t||!likelyDirectQuestion(t))return false;if(/[?؟]\s*$/.test(t))return true;
+ const n=normalizeAutoQuestion(t);if(n.split(' ').filter(Boolean).length<4)return false;
+ return /^(?:adam\s+)?(?:what|why|how|when|where|who|which|can|could|should|would|is|are|do|does|did|will|may|explain|tell)\b/i.test(n);
+}
 function sleepMeeting(ms){return new Promise(resolve=>setTimeout(resolve,ms));}
 function looksLikeLowInformationNoise(q){
  const raw=String(q||'').trim();if(!raw)return true;const n=normalizeAutoQuestion(raw);if(!n)return true;
@@ -214,6 +233,7 @@
  if(key&&key===meetingLastAutoQuestion&&now-meetingLastAutoAt<15000)return true;
  if(meetingAutoResponseBusy){queueAutomaticQuestion(q,questionId);return true;}
  meetingAutoResponseBusy=true;meetingAutoInFlightKey=key;document.getElementById('conversationInput').value=q;
+ meetingLatencyTrace.questionId=questionId;meetingLatencyTrace.dispatchAt=Date.now();renderMeetingLatencyTrace();
  setMeetingReplyTrace(2,'representative dispatch started',questionId);
  let handled=false;
  try{handled=await runInteractiveConversation(q,{questionId,attempt:0});if(handled){meetingLastAutoQuestion=key;meetingLastAutoAt=Date.now();}return !!handled;}finally{meetingAutoResponseBusy=false;if(meetingAutoInFlightKey===key)meetingAutoInFlightKey='';setTimeout(processAutomaticQuestionQueue,100);}
@@ -234,7 +254,8 @@
   const acceptedQuietFor=now-(meetingLastAcceptedTranscriptAt||now);
   const waitedFor=now-(meetingPendingAutoStartedAt||now);
   const waitingForTranscription=meetingTranscriptionsPending>0;
-  const requiredStability=finalBoundary?MEETING_FINAL_TURN_MERGE_GRACE_MS:MEETING_RESPONSE_STABILITY_MS;
+  const fastComplete=finalBoundary&&meetingTextLooksCompleteForFastReply(meetingPendingAutoParts.join(' '));
+  const requiredStability=finalBoundary?(fastComplete?MEETING_FINAL_TURN_FAST_COMPLETE_GRACE_MS:MEETING_FINAL_TURN_MERGE_GRACE_MS):MEETING_RESPONSE_STABILITY_MS;
   const participantContinuation=finalBoundary&&meetingVadAvailable&&meetingUtteranceSpeechStarted&&meetingUtteranceLastSpeechAt>0&&(now-meetingUtteranceLastSpeechAt)<MEETING_CONTINUATION_SPEECH_HOLD_MS;
   const stable=acceptedQuietFor>=requiredStability;
   const maxWait=finalBoundary?MEETING_FINAL_TURN_MAX_WAIT_MS:MEETING_MAX_AUTOREPLY_WAIT_MS;
@@ -247,7 +268,7 @@
   meetingPendingAutoParts=[];meetingPendingAutoQuestionId='';meetingPendingAutoTimer=null;meetingPendingAutoStartedAt=0;
   if(combined){micStatus.textContent=watchdog?'Microphone: response watchdog — Adam is answering captured interaction':'Microphone: participant turn complete — Adam is answering';triggerAutomaticConversation(combined,{questionId:questionIdForTurn});}
  };
- meetingPendingAutoTimer=setTimeout(check,Math.min(finalBoundary?MEETING_FINAL_TURN_MERGE_GRACE_MS:MEETING_RESPONSE_STABILITY_MS,700));
+ meetingPendingAutoTimer=setTimeout(check,Math.min(finalBoundary?(meetingTextLooksCompleteForFastReply(meetingPendingAutoParts.join(' '))?MEETING_FINAL_TURN_FAST_COMPLETE_GRACE_MS:MEETING_FINAL_TURN_MERGE_GRACE_MS):MEETING_RESPONSE_STABILITY_MS,700));
 }
 function normalizeEchoText(t){return String(t||'').toLowerCase().replace(/[\s\p{P}\p{S}]+/gu,' ').trim();}
 function looksLikeAdamSpeakerEcho(q){
@@ -377,8 +398,9 @@
  const fd=new FormData(); const mime=blob.type||'audio/webm'; const ext=mime.includes('mp4')?'m4a':'webm';
  fd.append('audio',blob,'meeting.'+ext);fd.append('owner_approved','true');fd.append('language',document.getElementById('meetingLanguage').value);fd.append('capture_ms',String(Math.max(0,Number(captureMs)||0)));fd.append('technical_context',(((document.getElementById('meetingFocusAgenda')||{}).value||'')+' '+((document.getElementById('ownerBriefing')||{}).value||'')+' '+transcript.value.slice(-1200)).slice(-2200));
  micStatus.textContent='Microphone: listening — transcribing multilingual speech…';
+ meetingLatencyTrace.sttStartAt=Date.now();renderMeetingLatencyTrace();
  try{
-  const r=await fetch('/api/personal-assistant/real-meeting-attendance/transcribe',{method:'POST',body:fd});const j=await r.json();
+  const r=await fetch('/api/personal-assistant/real-meeting-attendance/transcribe',{method:'POST',body:fd});const j=await r.json();meetingLatencyTrace.sttEndAt=Date.now();renderMeetingLatencyTrace();
   if(!r.ok||!j.ok)throw new Error(j.error||j.status||'meeting_transcription_failed');
   if(sourceSessionId!==meetingConversationSessionId||sourceSessionEpoch!==meetingSessionEpoch)return;
   if(j.text)await handleFinalMeetingText(j.text,{finalTurn:!!finalTurn,source:'microphone_server',sessionId:sourceSessionId,sessionEpoch:sourceSessionEpoch,peak_rms:Number(focusSignal.peak_rms||0),noise_floor:Number(focusSignal.noise_floor||0),vad_available:!!focusSignal.vad_available});
@@ -397,7 +419,7 @@
   meetingLevelTimer=setInterval(()=>{
    if(!meetingAnalyser||!listening)return;
    try{
-    if(meetingSpeakerProtectionEnabled()&&meetingSpeakerOutputActive){meetingRecorderContainsAdamAudio=true;meetingChunkSpeechFrames=0;meetingUtteranceSpeechStarted=false;meetingUtteranceLastSpeechAt=0;return;}
+    if(meetingSpeakerProtectionEnabled()&&meetingSpeakerOutputActive){meetingRecorderContainsAdamAudio=true;meetingChunkSpeechFrames=0;meetingUtteranceSpeechStarted=false;meetingUtteranceFirstSpeechAt=0;meetingUtteranceLastSpeechAt=0;return;}
     meetingAnalyser.getByteTimeDomainData(samples);let sum=0;
     for(let i=0;i<samples.length;i++){const x=(samples[i]-128)/128;sum+=x*x;}
     const rms=Math.sqrt(sum/samples.length);meetingPeakRms=Math.max(meetingPeakRms,rms);
@@ -406,8 +428,8 @@
     const threshold=Math.max(0.006,meetingNoiseFloor*2.2);
     if(rms>threshold){
      meetingChunkSpeechFrames++;
-     const now=Date.now();meetingUtteranceSpeechStarted=true;meetingUtteranceLastSpeechAt=now;meetingLastParticipantSpeechAt=now;
-     if(meetingChunkSpeechFrames===2)micStatus.textContent='Microphone: listening — speech detected; Adam will answer after a natural pause';
+     const now=Date.now();meetingUtteranceSpeechStarted=true;if(!meetingUtteranceFirstSpeechAt)meetingUtteranceFirstSpeechAt=now;meetingUtteranceLastSpeechAt=now;meetingLastParticipantSpeechAt=now;meetingLatencyTrace.speechEndAt=now;
+     if(meetingChunkSpeechFrames===2){meetingLatencyTrace={speechEndAt:now,sttStartAt:0,sttEndAt:0,dispatchAt:0,aiStartAt:0,aiEndAt:0,voiceStartAt:0,questionId:'',providerMs:0,fastPath:false};renderMeetingLatencyTrace();micStatus.textContent='Microphone: listening — speech detected; Adam will answer after a natural pause';}
     }else{
      meetingNoiseFloor=Math.max(0.001,Math.min(0.02,(meetingNoiseFloor*0.985)+(rms*0.015)));
     }
@@ -423,7 +445,7 @@
 function recordNextMultilingualChunk(){
  if(!listening||!meetingMicStream)return;
  const recorderSessionId=meetingConversationSessionId;const recorderSessionEpoch=meetingSessionEpoch;
- const chunks=[]; const mime=preferredMeetingMime();meetingChunkSpeechFrames=0;meetingPeakRms=0;meetingUtteranceSpeechStarted=false;meetingUtteranceLastSpeechAt=0;meetingRecorderStartedAt=Date.now();meetingRecorderStopReason='';meetingRecorderContainsAdamAudio=!!(meetingSpeakerProtectionEnabled()&&meetingSpeakerOutputActive);
+ const chunks=[]; const mime=preferredMeetingMime();meetingChunkSpeechFrames=0;meetingPeakRms=0;meetingUtteranceSpeechStarted=false;meetingUtteranceFirstSpeechAt=0;meetingUtteranceLastSpeechAt=0;meetingRecorderStartedAt=Date.now();meetingRecorderStopReason='';meetingRecorderContainsAdamAudio=!!(meetingSpeakerProtectionEnabled()&&meetingSpeakerOutputActive);
  try{meetingRecorder=new MediaRecorder(meetingMicStream,mime?{mimeType:mime}:undefined);}catch(e){micStatus.textContent='Microphone: MediaRecorder '+e.message;return;}
  meetingRecorder.ondataavailable=e=>{if(e.data&&e.data.size)chunks.push(e.data)};
  meetingRecorder.onstop=()=>{
@@ -442,7 +464,10 @@
  meetingChunkTimer=setInterval(()=>{
   if(!meetingRecorder||meetingRecorder.state!=='recording')return;
   const now=Date.now();const elapsed=now-meetingRecorderStartedAt;
-  const endOfTurn=meetingUtteranceSpeechStarted&&meetingChunkSpeechFrames>=2&&meetingUtteranceLastSpeechAt>0&&(now-meetingUtteranceLastSpeechAt)>=MEETING_END_OF_TURN_SILENCE_MS;
+  const spokenSpan=(meetingUtteranceFirstSpeechAt&&meetingUtteranceLastSpeechAt)?Math.max(0,meetingUtteranceLastSpeechAt-meetingUtteranceFirstSpeechAt):0;
+  const fastResponseEnabled=!!((document.getElementById('meetingFastResponse')||{}).checked);
+  const endOfTurnSilenceMs=(fastResponseEnabled&&spokenSpan>0&&spokenSpan<=MEETING_FAST_TURN_MAX_SPEECH_MS)?MEETING_FAST_END_OF_TURN_SILENCE_MS:MEETING_END_OF_TURN_SILENCE_MS;
+  const endOfTurn=meetingUtteranceSpeechStarted&&meetingChunkSpeechFrames>=2&&meetingUtteranceLastSpeechAt>0&&(now-meetingUtteranceLastSpeechAt)>=endOfTurnSilenceMs;
   const silentBound=!meetingUtteranceSpeechStarted&&elapsed>=MEETING_SILENT_CHUNK_MS;
   const maxBound=elapsed>=MEETING_MAX_UTTERANCE_MS;
   if(endOfTurn){meetingRecorderStopReason='end_of_turn';micStatus.textContent='Microphone: natural pause detected — transcribing complete question';try{meetingRecorder.stop();}catch(_){}return;}
@@ -477,7 +502,7 @@
 };
 document.getElementById('stop').onclick=()=>{stopMultilingualServerListening();if(recognition){try{recognition.stop();}catch(_){}}micStatus.textContent='Microphone: stopped';};
 document.getElementById('clear').onclick=()=>{transcript.value='';meetingPendingAutoParts=[];meetingPendingAutoQuestionId='';meetingPendingAutoStartedAt=0;meetingLastAcceptedTranscriptAt=0;if(meetingPendingAutoTimer){clearTimeout(meetingPendingAutoTimer);meetingPendingAutoTimer=null;}document.getElementById('conversationInput').value='';if(document.getElementById('meetingTextQuery'))document.getElementById('meetingTextQuery').value='';if(document.getElementById('meetingTextQueryStatus'))document.getElementById('meetingTextQueryStatus').textContent='Typed meeting input: ready';document.getElementById('conversationStatus').textContent='Conversation: transcript screen cleared — Adam retained meeting memory and reply history';document.getElementById('out').textContent='Screen cleared. Meeting memory retained; Adam\'s latest reply, reply history, and participant question history are retained until End Meeting / New Session.';};
-document.getElementById('endMeeting').onclick=async()=>{if(!confirm('End this meeting and clear Adam\'s temporary meeting memory for this session?'))return;const closingSessionId=meetingConversationSessionId;meetingSessionEpoch++;meetingRecognitionSessionEpoch=0;meetingRecognitionSessionId='';stopMultilingualServerListening();if(recognition){try{recognition.stop();}catch(_){}}stopAdamSpeechForBargeIn();if(meetingPendingAutoTimer){clearTimeout(meetingPendingAutoTimer);meetingPendingAutoTimer=null;}try{await meetingTranscriptionQueue.catch(()=>{});await meetingMemorySyncQueue.catch(()=>{});await fetch('/api/personal-assistant/meeting-conversation/reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({session_id:closingSessionId})});}catch(_){ }meetingConversationHistory=[];meetingQuestionQueue=[];meetingAutoResponseBusy=false;meetingAutoInFlightKey='';meetingPendingAutoParts=[];meetingPendingAutoQuestionId='';meetingPendingAutoStartedAt=0;meetingLastAcceptedTranscriptAt=0;meetingLastAutoQuestion='';meetingLastAutoAt=0;meetingLastFocusCandidateText='';meetingLastFocusCandidateAt=0;meetingResponseHistory=[];meetingQuestionHistory=[];meetingDiscussionHistory=[];meetingFocusAcceptedCount=0;meetingFocusRejectedCount=0;meetingFocusEvents=[];meetingAddressHoldUntil=0;meetingAwaitingClarification=false;meetingClarificationPrompt='';meetingClarificationAt=0;meetingClarificationQuestionId='';meetingConversationSessionId=(crypto&&crypto.randomUUID)?crypto.randomUUID():String(Date.now())+'-'+Math.random().toString(16).slice(2);meetingConversationHistorySessionId=meetingConversationSessionId;meetingRecognitionSessionId=meetingConversationSessionId;meetingRecognitionSessionEpoch=meetingSessionEpoch;meetingIdentityDeclared=false;meetingSpeakerOutputActive=false;meetingRecorderContainsAdamAudio=false;meetingLastVoiceDeliveryOk=false;meetingLastSpokenText='';meetingLastTranscriptText='';meetingLastTranscriptAt=0;transcript.value='';document.getElementById('conversationInput').value='';if(document.getElementById('meetingTextQuery'))document.getElementById('meetingTextQuery').value='';if(document.getElementById('meetingTextQueryStatus'))document.getElementById('meetingTextQueryStatus').textContent='Typed meeting input: ready';document.getElementById('responseDraft').value='';document.getElementById('responseHistory').value='';document.getElementById('questionHistory').value='';document.getElementById('discussionRecord').value='';document.getElementById('meetingRegister').value='';document.getElementById('questionCoverage').value='';if(document.getElementById('focusMonitor'))document.getElementById('focusMonitor').value='';document.getElementById('meetingReport').value='';document.getElementById('conversationStatus').textContent='Conversation: new meeting session ready — prior session isolated';document.getElementById('out').textContent='Previous meeting session ended. Late audio/AI callbacks from it will be ignored.';resetMeetingReplyTrace();micStatus.textContent='Microphone: stopped';};
+document.getElementById('endMeeting').onclick=async()=>{if(!confirm('End this meeting and clear Adam\'s temporary meeting memory for this session?'))return;const closingSessionId=meetingConversationSessionId;meetingSessionEpoch++;meetingRecognitionSessionEpoch=0;meetingRecognitionSessionId='';stopMultilingualServerListening();if(recognition){try{recognition.stop();}catch(_){}}stopAdamSpeechForBargeIn();if(meetingPendingAutoTimer){clearTimeout(meetingPendingAutoTimer);meetingPendingAutoTimer=null;}try{await meetingTranscriptionQueue.catch(()=>{});await meetingMemorySyncQueue.catch(()=>{});await fetch('/api/personal-assistant/meeting-conversation/reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({session_id:closingSessionId})});}catch(_){ }meetingConversationHistory=[];meetingQuestionQueue=[];meetingAutoResponseBusy=false;meetingAutoInFlightKey='';meetingPendingAutoParts=[];meetingPendingAutoQuestionId='';meetingPendingAutoStartedAt=0;meetingLastAcceptedTranscriptAt=0;meetingLastAutoQuestion='';meetingLastAutoAt=0;meetingLastFocusCandidateText='';meetingLastFocusCandidateAt=0;meetingResponseHistory=[];meetingQuestionHistory=[];meetingDiscussionHistory=[];meetingFocusAcceptedCount=0;meetingFocusRejectedCount=0;meetingFocusEvents=[];meetingAddressHoldUntil=0;meetingAwaitingClarification=false;meetingClarificationPrompt='';meetingClarificationAt=0;meetingClarificationQuestionId='';meetingConversationSessionId=(crypto&&crypto.randomUUID)?crypto.randomUUID():String(Date.now())+'-'+Math.random().toString(16).slice(2);meetingConversationHistorySessionId=meetingConversationSessionId;meetingRecognitionSessionId=meetingConversationSessionId;meetingRecognitionSessionEpoch=meetingSessionEpoch;meetingIdentityDeclared=false;meetingSpeakerOutputActive=false;meetingRecorderContainsAdamAudio=false;meetingLastVoiceDeliveryOk=false;meetingLastSpokenText='';meetingLastTranscriptText='';meetingLastTranscriptAt=0;transcript.value='';document.getElementById('conversationInput').value='';if(document.getElementById('meetingTextQuery'))document.getElementById('meetingTextQuery').value='';if(document.getElementById('meetingTextQueryStatus'))document.getElementById('meetingTextQueryStatus').textContent='Typed meeting input: ready';document.getElementById('responseDraft').value='';document.getElementById('responseHistory').value='';document.getElementById('questionHistory').value='';document.getElementById('discussionRecord').value='';document.getElementById('meetingRegister').value='';document.getElementById('questionCoverage').value='';if(document.getElementById('focusMonitor'))document.getElementById('focusMonitor').value='';document.getElementById('meetingReport').value='';document.getElementById('conversationStatus').textContent='Conversation: new meeting session ready — prior session isolated';document.getElementById('out').textContent='Previous meeting session ended. Late audio/AI callbacks from it will be ignored.';resetMeetingReplyTrace();resetMeetingLatencyTrace();micStatus.textContent='Microphone: stopped';};
 document.getElementById('analyze').onclick=async()=>{const text=transcript.value.trim();const segments=text?text.split(/\n+/).filter(Boolean).map(x=>({text:x})):[];const body={platform:document.getElementById('platform').value,mode:document.getElementById('mode').value,owner_approved:approved(),segments};const r=await fetch('/api/personal-assistant/real-meeting-attendance/process',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});document.getElementById('out').textContent=JSON.stringify(await r.json(),null,2);};
 
 document.getElementById('prepareResponse').onclick=async()=>{const body={mode:'active_participant',context:transcript.value,response:document.getElementById('responseDraft').value};const r=await fetch('/api/personal-assistant/meeting-conversation/prepare',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(j.response)document.getElementById('responseDraft').value=j.response;document.getElementById('speakStatus').textContent=j.ok?'Speaking: preview ready — approval required':'Speaking: '+j.status;};
@@ -487,7 +512,7 @@
 let meetingConversationHistorySessionId=meetingConversationSessionId;let meetingSessionEpoch=1;let meetingRecognitionSessionId=meetingConversationSessionId;let meetingRecognitionSessionEpoch=meetingSessionEpoch;
 function meetingVoiceEnabled(){const el=document.getElementById('meetingAutoSpeak');return !el||!!el.checked;}
 function meetingSpeakerProtectionEnabled(){const el=document.getElementById('meetingSpeakerProtection');return !el||!!el.checked;}
-function markAdamSpeakerPlaybackStart(){meetingSpeakerOutputActive=true;meetingRecorderContainsAdamAudio=true;meetingChunkSpeechFrames=0;meetingUtteranceSpeechStarted=false;meetingUtteranceLastSpeechAt=0;setMeetingVoiceStatus('speaking — laptop speaker protection active');}
+function markAdamSpeakerPlaybackStart(){meetingSpeakerOutputActive=true;meetingRecorderContainsAdamAudio=true;meetingChunkSpeechFrames=0;meetingUtteranceSpeechStarted=false;meetingUtteranceFirstSpeechAt=0;meetingUtteranceLastSpeechAt=0;if(!meetingLatencyTrace.voiceStartAt){meetingLatencyTrace.voiceStartAt=Date.now();renderMeetingLatencyTrace();}setMeetingVoiceStatus('speaking — laptop speaker protection active');}
 function restartCleanMeetingCaptureAfterAdamSpeech(){
  meetingSpeakerOutputActive=false;
  if(!meetingSpeakerProtectionEnabled()||!listening)return;
@@ -520,12 +545,12 @@
  for(const raw of sentences){const sentence=raw.trim();if(!sentence)continue;if(sentence.length>maxChars){if(buf){out.push(buf);buf='';}pushHard(sentence);continue;}if(!buf)buf=sentence;else if((buf+' '+sentence).length<=maxChars)buf+=' '+sentence;else{out.push(buf);buf=sentence;}}
  if(buf)out.push(buf);return out;
 }
-function browserSpeakMeetingText(text,language,jobId=meetingVoiceJobSeq){
+function browserSpeakMeetingText(text,language,jobId=meetingVoiceJobSeq,finalChunk=true){
  return new Promise((resolve,reject)=>{
   if(!('speechSynthesis' in window)){reject(new Error('browser speech unavailable'));return;}
   try{
    speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang=String(language||'en-US');const v=preferredBrowserMeetingVoice(u.lang);if(v)u.voice=v;u.volume=1;u.rate=1;u.pitch=1;
-   let done=false;let speakerStarted=false;const finish=(msg,err)=>{if(done)return;done=true;clearTimeout(timer);meetingAdamSpeechResolve=null;if(speakerStarted){speakerStarted=false;restartCleanMeetingCaptureAfterAdamSpeech();}if(err)reject(err);else resolve(msg);};
+   let done=false;let speakerStarted=false;const finish=(msg,err)=>{if(done)return;done=true;clearTimeout(timer);meetingAdamSpeechResolve=null;if(speakerStarted){speakerStarted=false;if(finalChunk)restartCleanMeetingCaptureAfterAdamSpeech();else meetingSpeakerOutputActive=true;}if(err)reject(err);else resolve(msg);};
    const timer=setTimeout(()=>finish('',new Error('browser voice timeout')),Math.max(12000,Math.min(45000,String(text||'').length*95)));
    meetingAdamSpeechResolve=(msg)=>finish(msg||'interrupted',null);u.onend=()=>finish('browser voice chunk completed',null);u.onerror=()=>finish('',new Error('browser voice error'));
    if(jobId!==meetingVoiceJobSeq){finish('stale voice job',null);return;}markAdamSpeakerPlaybackStart();speakerStarted=true;speechSynthesis.speak(u);
@@ -533,13 +558,14 @@
  });
 }
 async function playServerMeetingChunk(text,locale,jobId,chunkIndex,chunkCount){
+ const finalChunk=Number(chunkIndex)>=Number(chunkCount);
  const controller=('AbortController' in window)?new AbortController():null;const timeout=setTimeout(()=>{try{if(controller)controller.abort();}catch(_){}},12000);
  try{
   setMeetingVoiceStatus('preparing Adam voice — part '+chunkIndex+' of '+chunkCount);
   const a=await fetch('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,language:locale}),signal:controller?controller.signal:undefined});
   if(!a.ok)throw new Error('TTS unavailable');const blob=await a.blob();if(!blob||!blob.size)throw new Error('empty TTS audio');if(jobId!==meetingVoiceJobSeq)throw new Error('stale voice job');
   meetingAdamAudioUrl=URL.createObjectURL(blob);meetingAdamAudio=new Audio(meetingAdamAudioUrl);meetingAdamAudio.preload='auto';meetingAdamAudio.volume=1;meetingAdamAudio.muted=false;meetingAdamAudio.playsInline=true;
-  return await new Promise((resolve,reject)=>{let settled=false;let speakerStarted=false;const finish=(msg,err)=>{if(settled)return;settled=true;clearTimeout(watchdog);const url=meetingAdamAudioUrl;meetingAdamAudio=null;meetingAdamAudioUrl='';meetingAdamSpeechResolve=null;if(speakerStarted){speakerStarted=false;restartCleanMeetingCaptureAfterAdamSpeech();}if(url)try{URL.revokeObjectURL(url);}catch(_){}if(err)reject(err);else resolve(msg);};meetingAdamSpeechResolve=(msg)=>finish(msg||'interrupted',null);meetingAdamAudio.onended=()=>finish('server voice chunk completed',null);meetingAdamAudio.onerror=()=>finish('',new Error('TTS playback failed'));const watchdog=setTimeout(()=>finish('',new Error('TTS playback timeout')),Math.max(15000,Math.min(45000,text.length*100)));meetingAdamAudio.play().then(()=>{meetingVoiceUnlocked=true;markAdamSpeakerPlaybackStart();speakerStarted=true;setMeetingVoiceStatus('speaking — part '+chunkIndex+' of '+chunkCount+' — speaker echo protected');}).catch(e=>finish('',e));});
+  return await new Promise((resolve,reject)=>{let settled=false;let speakerStarted=false;const finish=(msg,err)=>{if(settled)return;settled=true;clearTimeout(watchdog);const url=meetingAdamAudioUrl;meetingAdamAudio=null;meetingAdamAudioUrl='';meetingAdamSpeechResolve=null;if(speakerStarted){speakerStarted=false;if(finalChunk)restartCleanMeetingCaptureAfterAdamSpeech();else meetingSpeakerOutputActive=true;}if(url)try{URL.revokeObjectURL(url);}catch(_){}if(err)reject(err);else resolve(msg);};meetingAdamSpeechResolve=(msg)=>finish(msg||'interrupted',null);meetingAdamAudio.onended=()=>finish('server voice chunk completed',null);meetingAdamAudio.onerror=()=>finish('',new Error('TTS playback failed'));const watchdog=setTimeout(()=>finish('',new Error('TTS playback timeout')),Math.max(15000,Math.min(45000,text.length*100)));meetingAdamAudio.play().then(()=>{meetingVoiceUnlocked=true;markAdamSpeakerPlaybackStart();speakerStarted=true;setMeetingVoiceStatus('speaking — part '+chunkIndex+' of '+chunkCount+' — speaker echo protected');}).catch(e=>finish('',e));});
  }finally{clearTimeout(timeout);}
 }
 async function playMeetingBlobWithUnlockedAudioContext(blob){
@@ -564,36 +590,39 @@
  const clean=speechFriendlyMeetingText(original);if(!clean)return 'no voice text';
  const locale=String(language||(/[\u0600-\u06ff]/.test(clean)?'ar-LB':'auto'));
  meetingLastSpokenText=clean;meetingLastSpokenAt=Date.now();meetingAdamSpeaking=true;
- meetingSpeakerOutputActive=true;meetingRecorderContainsAdamAudio=true;
- setMeetingVoiceStatus('preparing automatic meeting voice');
+ const fastVoice=!!((document.getElementById('meetingFastVoice')||{}).checked);
+ const voiceChunks=fastVoice?splitMeetingSpeechChunks(clean,MEETING_FAST_VOICE_CHUNK_CHARS):[clean];
+ if(fastVoice&&voiceChunks.length>1){
+  const jobId=++meetingVoiceJobSeq;meetingSpeakerOutputActive=false;meetingRecorderContainsAdamAudio=false;setMeetingVoiceStatus('fast voice — preparing first short speech chunk');
+  try{
+   for(let i=0;i<voiceChunks.length;i++){
+    const chunk=voiceChunks[i],finalChunk=(i===voiceChunks.length-1);
+    try{await playServerMeetingChunk(chunk,locale,jobId,i+1,voiceChunks.length);}
+    catch(serverChunkErr){setMeetingVoiceStatus('server chunk unavailable — browser voice fallback');await browserSpeakMeetingText(chunk,locale,jobId,finalChunk);}
+   }
+   meetingLastVoiceDeliveryOk=true;setMeetingVoiceStatus('voice completed — fast chunked playback');return 'voice completed';
+  }finally{meetingAdamSpeaking=false;meetingSpeakerOutputActive=false;meetingRecorderContainsAdamAudio=false;meetingLastSpokenAt=Date.now();}
+ }
+ meetingSpeakerOutputActive=true;meetingRecorderContainsAdamAudio=true;setMeetingVoiceStatus('preparing automatic meeting voice');
  try{
   const r=await fetch('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:clean,language:locale})});
   if(!r.ok)throw new Error('tts unavailable');
   const blob=await r.blob();if(!blob||!blob.size)throw new Error('empty tts audio');
-  // v8.4.1.1: play through the AudioContext that was unlocked by Start Listening / Test Voice.
-  // This avoids browser autoplay restrictions that can block a later async meeting reply even though the short button-click test works.
   try{
-   setMeetingVoiceStatus('speaking — unlocked meeting audio context');
-   await playMeetingBlobWithUnlockedAudioContext(blob);
+   setMeetingVoiceStatus('speaking — unlocked meeting audio context');markAdamSpeakerPlaybackStart();
+   await playMeetingBlobWithUnlockedAudioContext(blob);restartCleanMeetingCaptureAfterAdamSpeech();
    meetingLastVoiceDeliveryOk=true;setMeetingVoiceStatus('voice completed — ready for next participant turn');return 'voice completed';
-  }catch(contextErr){
-   setMeetingVoiceStatus('audio-context playback unavailable — trying HTML audio');
-  }
+  }catch(contextErr){setMeetingVoiceStatus('audio-context playback unavailable — trying HTML audio');}
   const url=URL.createObjectURL(blob);const audio=new Audio(url);meetingAdamAudio=audio;meetingAdamAudioUrl=url;audio.preload='auto';audio.volume=1;audio.muted=false;
-  await new Promise((resolve,reject)=>{let finished=false;const done=(err)=>{if(finished)return;finished=true;meetingAdamAudio=null;meetingAdamAudioUrl='';try{URL.revokeObjectURL(url);}catch(_){}err?reject(err):resolve();};audio.onended=()=>done();audio.onerror=()=>done(new Error('audio playback failed'));audio.play().then(()=>{meetingVoiceUnlocked=true;}).catch(done);});
+  await new Promise((resolve,reject)=>{let finished=false;const done=(err)=>{if(finished)return;finished=true;meetingAdamAudio=null;meetingAdamAudioUrl='';try{URL.revokeObjectURL(url);}catch(_){}restartCleanMeetingCaptureAfterAdamSpeech();err?reject(err):resolve();};audio.onended=()=>done();audio.onerror=()=>done(new Error('audio playback failed'));audio.play().then(()=>{meetingVoiceUnlocked=true;markAdamSpeakerPlaybackStart();}).catch(done);});
   meetingLastVoiceDeliveryOk=true;setMeetingVoiceStatus('voice completed — HTML audio fallback');return 'voice completed';
  }catch(serverErr){
   if('speechSynthesis' in window){
-   try{
-    setMeetingVoiceStatus('server audio unavailable — speaking with browser voice');
-    await new Promise((resolve,reject)=>{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(clean);if(locale&&locale!=='auto')u.lang=locale;const v=preferredBrowserMeetingVoice(locale==='auto'?'en-US':locale);if(v)u.voice=v;u.volume=1;u.rate=1;u.pitch=1;let done=false;const finish=(err)=>{if(done)return;done=true;err?reject(err):resolve();};u.onend=()=>finish();u.onerror=()=>finish(new Error('browser voice error'));speechSynthesis.speak(u);});
-    meetingLastVoiceDeliveryOk=true;setMeetingVoiceStatus('voice completed — browser fallback');return 'voice completed';
-   }catch(browserErr){setMeetingVoiceStatus('audio unavailable — all three playback paths failed');throw browserErr;}
+   try{setMeetingVoiceStatus('server audio unavailable — speaking with browser voice');await browserSpeakMeetingText(clean,locale,meetingVoiceJobSeq,true);meetingLastVoiceDeliveryOk=true;setMeetingVoiceStatus('voice completed — browser fallback');return 'voice completed';}
+   catch(browserErr){setMeetingVoiceStatus('audio unavailable — all three playback paths failed');throw browserErr;}
   }
   setMeetingVoiceStatus('audio unavailable — server voice failed and browser voice unavailable');throw serverErr;
- }finally{
-  meetingAdamSpeaking=false;meetingSpeakerOutputActive=false;meetingRecorderContainsAdamAudio=false;meetingLastSpokenAt=Date.now();
- }
+ }finally{meetingAdamSpeaking=false;meetingSpeakerOutputActive=false;meetingRecorderContainsAdamAudio=false;meetingLastSpokenAt=Date.now();}
 }
 async function runInteractiveConversation(question,options={}){
  const status=document.getElementById('conversationStatus');
@@ -614,13 +643,13 @@
  status.textContent=attempt?'Conversation: retrying the participant interaction…':'Conversation: Adam is thinking…';
  const context=transcript.value.slice(-6000);
  let r,j;
- setMeetingReplyTrace(3,'waiting for AI provider',questionId);
+ setMeetingReplyTrace(3,'waiting for AI provider',questionId);meetingLatencyTrace.aiStartAt=Date.now();renderMeetingLatencyTrace();
  let providerWatchdog=null;const providerController=(typeof AbortController!=='undefined')?new AbortController():null;
  try{
   if(providerController)providerWatchdog=setTimeout(()=>providerController.abort(),30000);
   r=await fetch('/api/personal-assistant/meeting-conversation/ask',{method:'POST',headers:{'Content-Type':'application/json'},signal:providerController?providerController.signal:undefined,body:JSON.stringify({question,question_id:questionId,direct_question:directQuestion,turn_kind:turnKind,response_required:responseRequired,clarification_followup:clarificationFollowup,clarification_parent_question_id:meetingClarificationQuestionId,meeting_context:context,messages:meetingConversationHistory.slice(-10),client_history_session_id:meetingConversationHistorySessionId,session_id:sessionIdAtDispatch,owner_approved:sessionApproved,representative_mode:representativeMode,owner_briefing:(document.getElementById('ownerBriefing')||{}).value||'',authority_profile:(document.getElementById('authorityProfile')||{}).value||'',professional_scope:(document.getElementById('professionalScope')||{}).value||'auto',participation_style:(document.getElementById('participationStyle')||{}).value||'balanced',proactive_reason:proactiveInterventionReason(question),meeting_title:(document.getElementById('meetingTitle')||{}).value||'',project_reference:(document.getElementById('projectReference')||{}).value||'',participants:(document.getElementById('meetingParticipants')||{}).value||'',focus_agenda:(document.getElementById('meetingFocusAgenda')||{}).value||'',focus_locked:meetingFocusLocked(),focus_sensitivity:(document.getElementById('focusSensitivity')||{}).value||'strict',continuity_memory:document.getElementById('meetingContinuityMemory').checked,language:document.getElementById('meetingLanguage').value})});
-  j=await r.json().catch(()=>({}));
- }catch(e){const aborted=!!(e&&e.name==='AbortError');j={status:aborted?'client_ai_provider_timeout':'meeting_ai_request_failed',error:String(e&&e.message||e)};r={ok:false};}
+  j=await r.json().catch(()=>({}));meetingLatencyTrace.aiEndAt=Date.now();meetingLatencyTrace.providerMs=Number(j&&j.provider_elapsed_ms||0);meetingLatencyTrace.fastPath=!!(j&&j.latency_fast_path);renderMeetingLatencyTrace();
+ }catch(e){meetingLatencyTrace.aiEndAt=Date.now();renderMeetingLatencyTrace();const aborted=!!(e&&e.name==='AbortError');j={status:aborted?'client_ai_provider_timeout':'meeting_ai_request_failed',error:String(e&&e.message||e)};r={ok:false};}
  finally{if(providerWatchdog)clearTimeout(providerWatchdog);}
  if(sessionIdAtDispatch!==meetingConversationSessionId||sessionEpochAtDispatch!==meetingSessionEpoch){status.textContent='Conversation: stale prior-meeting response ignored';return false;}
  if(j&&j.session_id&&String(j.session_id)!==sessionIdAtDispatch){status.textContent='Conversation: mismatched meeting-session response ignored';return false;}
--- /dev/null	2026-09-10 19:10:50.879286831 +0000
+++ /mnt/data/adam8419/tests/test_meeting_response_latency_v8419.py	2026-09-10 20:31:16.215552866 +0000
@@ -0,0 +1,125 @@
+from pathlib import Path
+import ast
+import re
+
+ROOT = Path(__file__).resolve().parents[1]
+APP = (ROOT / "app.py").read_text(encoding="utf-8")
+HTML = (ROOT / "templates" / "real_meeting_attendance.html").read_text(encoding="utf-8")
+
+
+def _load_fast_helper():
+    tree = ast.parse(APP)
+    node = next(
+        n for n in tree.body
+        if isinstance(n, ast.FunctionDef) and n.name == "_meeting_latency_fast_factual_turn"
+    )
+    module = ast.Module(body=[node], type_ignores=[])
+    ast.fix_missing_locations(module)
+    ns = {"re": re}
+    exec(compile(module, str(ROOT / "app.py"), "exec"), ns, ns)
+    return ns["_meeting_latency_fast_factual_turn"]
+
+
+def test_version_bumped():
+    assert 'VERSION = "v8.4.1.9"' in APP
+
+
+def test_locked_end_of_turn_boundaries_are_preserved():
+    # v8.4.1.6 owner-approved protection remains unchanged.
+    assert "MEETING_END_OF_TURN_SILENCE_MS=4200" in HTML
+    assert "MEETING_FINAL_TURN_MERGE_GRACE_MS=1500" in HTML
+    assert "MEETING_CONTINUATION_SPEECH_HOLD_MS=900" in HTML
+
+
+def test_adaptive_fast_end_of_turn_preserves_full_pause_guard():
+    assert 'id="meetingFastResponse" type="checkbox" checked' in HTML
+    assert "MEETING_FAST_END_OF_TURN_SILENCE_MS=2400" in HTML
+    assert "MEETING_FAST_TURN_MAX_SPEECH_MS=6500" in HTML
+    assert "MEETING_END_OF_TURN_SILENCE_MS=4200" in HTML
+    assert "endOfTurnSilenceMs=(fastResponseEnabled&&spokenSpan>0&&spokenSpan<=MEETING_FAST_TURN_MAX_SPEECH_MS)?MEETING_FAST_END_OF_TURN_SILENCE_MS:MEETING_END_OF_TURN_SILENCE_MS" in HTML
+
+
+def test_fast_post_stt_grace_is_only_for_complete_direct_questions():
+    assert "MEETING_FINAL_TURN_FAST_COMPLETE_GRACE_MS=350" in HTML
+    assert "function meetingTextLooksCompleteForFastReply" in HTML
+    assert "!likelyDirectQuestion(t)" in HTML
+    assert "fastComplete=finalBoundary&&meetingTextLooksCompleteForFastReply" in HTML
+    assert "fastComplete?MEETING_FINAL_TURN_FAST_COMPLETE_GRACE_MS:MEETING_FINAL_TURN_MERGE_GRACE_MS" in HTML
+
+
+def test_latency_diagnostics_cover_all_major_stages():
+    assert 'id="meetingLatencyStatus"' in HTML
+    for token in (
+        "speechEndAt", "sttStartAt", "sttEndAt", "dispatchAt",
+        "aiStartAt", "aiEndAt", "voiceStartAt", "providerMs",
+    ):
+        assert token in HTML
+    for label in ("total to voice start", "end-turn", "STT", "focus/merge", "AI", "voice prep", "provider"):
+        assert label in HTML
+
+
+def test_short_factual_question_uses_fast_server_path():
+    helper = _load_fast_helper()
+    assert helper("Adam, what is the function of a balancing valve?", True, False, False, "question", "") is True
+    assert helper("What is the purpose of an expansion tank?", True, False, False, "question", "") is True
+
+
+def test_approval_and_change_questions_never_use_fast_server_path():
+    helper = _load_fast_helper()
+    assert helper("Can you approve us to proceed with the 100 mm chilled-water pipe?", True, False, False, "question", "") is False
+    assert helper("Should we change the chilled-water pump?", True, False, False, "question", "") is False
+    assert helper("What is the approved static pressure for this project?", True, False, False, "question", "") is False
+
+
+def test_contextual_and_clarification_turns_keep_full_path():
+    helper = _load_fast_helper()
+    q = "What is the function of the balancing valve?"
+    assert helper(q, True, True, False, "question", "") is False
+    assert helper(q, True, False, True, "question", "") is False
+    assert helper(q, True, False, False, "question", "risk intervention") is False
+
+
+def test_fast_prompt_has_compact_budget_without_touching_provider_transport():
+    assert "latency_fast_path = _meeting_latency_fast_factual_turn" in APP
+    assert "meeting_max_tokens = 320 if latency_fast_path else 700" in APP
+    assert "raw_answer = call_ai(prompt, reply_language, max_tokens=meeting_max_tokens, timeout=25).strip()" in APP
+    assert "provider_elapsed_ms" in APP
+    assert '"latency_fast_path": latency_fast_path' in APP
+    assert '"meeting_max_tokens": meeting_max_tokens' in APP
+
+
+def test_fast_prompt_preserves_owner_governance():
+    fast_block = APP[APP.index('if latency_fast_path:'):APP.index('# v8.4.1.8 — deterministic decision-critical clarification gate')]
+    assert "Never invent approvals" in fast_block
+    assert "Never approve, authorize" in fast_block
+    assert "Mohamad's behalf" in fast_block
+
+
+def test_fast_voice_uses_short_chunks_and_starts_timing_on_actual_playback():
+    assert "MEETING_FAST_VOICE_CHUNK_CHARS=140" in HTML
+    assert "splitMeetingSpeechChunks(clean,MEETING_FAST_VOICE_CHUNK_CHARS)" in HTML
+    assert "fast voice — preparing first short speech chunk" in HTML
+    assert "playServerMeetingChunk(chunk,locale,jobId,i+1,voiceChunks.length)" in HTML
+    assert "meetingLatencyTrace.voiceStartAt=Date.now()" in HTML
+
+
+def test_chunked_voice_keeps_speaker_protection_until_final_chunk():
+    assert "const finalChunk=Number(chunkIndex)>=Number(chunkCount);" in HTML
+    assert "if(finalChunk)restartCleanMeetingCaptureAfterAdamSpeech();else meetingSpeakerOutputActive=true" in HTML
+    assert "browserSpeakMeetingText(text,language,jobId=meetingVoiceJobSeq,finalChunk=true)" in HTML
+
+
+def test_existing_reply_trace_contract_is_preserved():
+    for label in (
+        "speech accepted", "representative dispatch started", "waiting for AI provider",
+        "AI response received", "spoken reply completed",
+    ):
+        assert label in HTML
+
+
+def test_prior_clarification_and_session_isolation_guards_remain_present():
+    assert "_meeting_material_change_clarification" in APP
+    assert "approval_status_consistency_guard" in APP
+    assert "meeting_session_closed" in APP
+    assert "stale prior-meeting response ignored" in HTML
+    assert "meetingSessionEpoch" in HTML
--- /dev/null	2026-09-10 19:10:50.879286831 +0000
+++ /mnt/data/adam8419/ACQUISITION_V8.4.1.9_NOTES.md	2026-09-10 20:31:41.278820970 +0000
@@ -0,0 +1,43 @@
+# Adam Acquisition v8.4.1.9 — Meeting Response Latency Fast Path + Timing Diagnostics
+
+## Purpose
+This focused upgrade addresses the live-meeting response delay reported after v8.4.1.8. A short meeting question was taking about 12 seconds before Adam began speaking.
+
+The upgrade reduces avoidable delay while preserving the previously approved v8.4.1.6 protection against answering before the participant finishes speaking.
+
+## Changes
+- Added **Fast meeting response** mode, enabled by default.
+- The original v8.4.1.6 full natural-pause boundary of **4200 ms** remains in the code and is still used for longer turns.
+- Short spoken turns may use an adaptive **2400 ms** silence boundary; once the spoken turn becomes longer than 6.5 seconds, Adam automatically falls back to the full 4200 ms pause protection.
+- After server transcription, a clearly complete direct question can use a **350 ms** final merge grace instead of the normal 1500 ms continuation grace. Incomplete/non-question turns retain the longer merge protection.
+- Added a narrow **low-latency factual-question server path** for short self-contained questions such as “What is the function of a balancing valve?”
+- Approval, proceed/change, cost, variation, commitment, contextual follow-up, clarification, and proactive-intervention turns are excluded from the compact path and continue using the full meeting prompt/governance logic.
+- The compact factual path keeps the same configured AI provider/model but reduces the normal response budget to 320 tokens and sends a smaller, governance-preserving prompt/context package.
+- Added **Meeting response latency** diagnostics showing, when available:
+  - total time from participant speech end to Adam voice start;
+  - end-of-turn wait;
+  - speech transcription time;
+  - focus/merge time;
+  - AI round-trip time;
+  - provider time returned by the server; and
+  - TTS/voice preparation time.
+- Activated the existing **Fast meeting voice** behavior for real replies: long answers are now synthesized in short speech chunks so Adam can begin speaking after the first short chunk is ready rather than waiting for the entire answer audio to be generated.
+- Voice playback timing is recorded at the moment actual speaker playback begins, not when TTS preparation starts.
+- Existing five-stage meeting reply trace remains unchanged, ending with **spoken reply completed**.
+- v8.4.1.5 AI-response, v8.4.1.6 end-of-turn/transcription, v8.4.1.7 approval consistency, and v8.4.1.8 clarification/session-isolation behavior remain present.
+
+## Targeted owner acceptance test
+Do not repeat previous locked acceptance tests.
+
+Start a clean meeting, keep **Fast meeting response** and **Fast meeting voice** enabled, then ask only:
+
+> Adam, what is the function of a balancing valve?
+
+PASS requires:
+- Adam does not interrupt before the sentence is finished.
+- One direct question is recorded, with one reply and no duplicate.
+- Adam begins speaking materially faster than the previously observed ~12 seconds.
+- The new **Meeting response latency** line shows the measured breakdown.
+- The normal five-stage trace still reaches **spoken reply completed**.
+
+The first physical run is used to establish the real machine/network result. If the total is still high, use the displayed stage timings to optimize only the slow stage in the next focused version rather than weakening turn-completion protection.
