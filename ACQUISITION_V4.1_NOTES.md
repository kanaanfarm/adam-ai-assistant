# Adam Acquisition v4.1.0 — Live Browser Control

New capability only: a governed local Selenium browser driver for installed Microsoft Edge or Google Chrome.

- HTTP/HTTPS navigation only.
- Page observation is bounded.
- CSS-targeted navigation/click contract.
- Typing into live applications requires explicit owner approval.
- Button/submit-like clicks require explicit owner approval.
- Buyer evidence excludes URLs, page text, typed values and credentials.
- Self-test is synthetic and performs no external network/application control.

Next: screen perception loop and adaptive recovery, then Meeting Agent.
