# Adam v6.0.0 — Real Daily Assistant Workflow

Adds a privacy-safe daily assistant workflow planner that converts a natural-language task into an Understand → Plan → Prepare → Owner Approval → Execute → Audit → Follow-up structure.

Acceptance proposal:
- Test 196: Daily workflow boundary/self-test.
- Test 197: Interactive multi-step plan; consequential steps must require Owner Approval and no real action is executed by the planner.

Previous Tests 1–195 remain locked PASS and are not repeated.
