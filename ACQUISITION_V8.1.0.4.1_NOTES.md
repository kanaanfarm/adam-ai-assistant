# Adam Acquisition v8.1.0.4.1

Guest Session Initialization Fix.

- Fixed JavaScript syntax regression in the v8.1.0.4 reliable voice controller.
- Guest status `load()` now executes normally instead of remaining at Checking session.
- Added explicit same-origin credentials and no-store to session status fetch.
- Added visible session-check error handling so initialization cannot fail silently.
- Preserves v8.1.0.4 reliable voice queue, factual grounding, Lebanese Arabic, privacy and approval boundaries.
