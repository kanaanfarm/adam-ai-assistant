# Adam Acquisition v2.8.0 — Voice AI Transport Boundary
Extracts Adam's online speech-synthesis provider transport into `adam_core.voice_ai_transport` while preserving `/api/tts` and its Windows/browser fallback behavior. Buyer evidence exposes no API keys, speech text, instructions, audio bytes, contacts, or private memory.
